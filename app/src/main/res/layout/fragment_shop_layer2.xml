<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.project.ecommerce.shopfragmentlayer2.presentation.ShopFragmentLayer2ViewModel" />
    </data>

    <FrameLayout
        android:id="@+id/shop_layer2_fragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".shopfragmentlayer2.presentation.ShopLayer2Fragment"
        android:background="@color/Background">
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">
            <include
                android:id="@+id/toolBar"
                layout="@layout/toolbar"/>
            <Button
                android:id="@+id/viewAllItemsBtn"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                app:layout_constraintTop_toBottomOf="@+id/toolBar"
                android:layout_margin="16dp"
                android:text="@string/view_all_items"
                android:textAllCaps="true"
                android:textSize="14sp"
                android:background="@drawable/btn1_bg"
                app:backgroundTint="@color/Primary"/>
            <TextView
                android:id="@+id/choose_category"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/viewAllItemsBtn"
                android:layout_marginStart="16dp"
                android:layout_marginTop="16dp"
                android:text="@string/choose_category"
                android:textSize="14sp"
                android:textColor="@color/Gray"/>
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/categoriesRecycler"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                app:layout_constraintTop_toBottomOf="@+id/choose_category"
                app:layout_constraintBottom_toBottomOf="parent"/>
        </androidx.constraintlayout.widget.ConstraintLayout>
    </FrameLayout>
</layout>